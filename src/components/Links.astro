---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"div">;

const {
  props: { class: className, ...props }
} = Astro;
---

<div class:list={["*:flex", className]} {...props}><slot /></div>
