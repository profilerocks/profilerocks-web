---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"ul">;

const {
  props: { class: className, ...props }
} = Astro;
---

<ul class:list={["list-disc space-y-1.5 ps-8 pe-2 text-zinc-400 marker:text-emerald-400", className]} {...props}><slot /></ul>
