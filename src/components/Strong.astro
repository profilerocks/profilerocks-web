---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"strong">;

const {
  props: { class: className, ...props }
} = Astro;
---

<strong class:list={["font-medium text-zinc-300", className]} {...props}>
  <slot />
</strong>
